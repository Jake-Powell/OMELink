% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_name_new.R
\name{score_name_matches}
\alias{score_name_matches}
\title{Score Combined First-Name and Last-Name Matches}
\usage{
score_name_matches(
  partial_matching,
  w_exact_both = 5,
  w_exact_token_both = 3,
  w_partial_start_both = 2,
  w_partial_anywhere_both = 1,
  w_initial_both = 0.5,
  w_fuzzy_mean = 3
)
}
\arguments{
\item{partial_matching}{Data frame returned by
\code{matching_breakdown()}.}

\item{w_exact_both}{Numeric weight applied when both first and last names
exactly match.}

\item{w_exact_token_both}{Numeric weight applied when both first and last
names have exact token matches.}

\item{w_partial_start_both}{Numeric weight applied when both first and
last names partially match at the start of a token.}

\item{w_partial_anywhere_both}{Numeric weight applied when both first and
last names partially match anywhere within a token.}

\item{w_initial_both}{Numeric weight applied when both first and last
names share token initials.}

\item{w_fuzzy_mean}{Numeric weight applied to the mean first-name and
last-name fuzzy similarity.}
}
\value{
The input data frame with additional columns:
  \describe{
    \item{mean_fuzzy}{Mean of \code{FN_fuzzy} and \code{LN_fuzzy}.}
    \item{match_score}{Composite matching score.}
    \item{match_confidence}{Scaled confidence between \code{0} and
      \code{1}.}
  }
}
\description{
Combines separate first-name and last-name matching signals into a single
composite score and a scaled confidence measure between \code{0} and
\code{1}.
}
\details{
The score is heuristic rather than probabilistic. It is intended to rank
candidate matches and provide an interpretable confidence-like measure.
}
\examples{
people = data.frame(
  FN = c("MATTHEW", "MATT"),
  LN = c("JONES", "JONES"),
  stringsAsFactors = FALSE
)

x = matching_breakdown("Matt", "Jones", people, "FN", "LN")
score_name_matches(x)

}
